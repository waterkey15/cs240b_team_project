#include "search.hh"

template<class T>

Search<T>::Search(vector<T> p1):data(p1){}

template<class T>

Search<T>::Search(){};

template<class T>

void Search<T>::AddItem(T item){};

template<class T>

T Search<T>::Find(std::string key){};